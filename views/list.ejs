<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>To Do List v1</title>
  </head>
  <body>
    <h1>Today is <%= specificDay %> and its <%= dayToday %></h1>
    <form action="/" method="post">
      <input type="text" name="toDoList" />
      <button type="submit" name="button">Add</button>
    </form>
    <ul>
      <% for (var i = 1; i < newListItem.length; i++) { %>
      <li><%= newListItem[i] %></li>
      <% } %>
    </ul>
  </body>
</html>
