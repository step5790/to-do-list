<!DOCTYPE html>
<html lang="en">
  <link
    rel="stylesheet"
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
  />
  <link rel="stylesheet" href="css/style.css" />
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>To Do List v1</title>
  </head>
  <body>
    <div class="p-5 mb-4 bg-body-tertiary rounded-3">
      <div class="container-fluid py-5 col-8">
        <h1 class="display-5 fw-bold">Today is <%= dayToday %></h1>

        <div class="display-5 fw-bold mt-5">
          <h3>To do list:</h3>

          <form action="/" method="post" class="form col-4 text-center mb-5">
            <div class="input-group mb-3">
              <input
                name="toDoList"
                type="text"
                class="form-control"
                placeholder="Add list"
                required="true"
              />
              <button
                class="btn btn-outline-secondary"
                type="submit"
                name="button"
                id="button-addon2"
              >
                +
              </button>
            </div>
          </form>
          <ul>
            <% for (var i = 1; i < newListItem.length; i++) { %>
            <li>
              <input
                class="form-check-input me-4"
                type="checkbox"
                value=""
                id="flexCheckDefault"
              /><label><%= newListItem[i] %></label>
            </li>
            <% } %>
          </ul>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js
    "></script>
  </body>
</html>
